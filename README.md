# uid-final-project

## First Priorities
|- need to store the user's answers
|- what time a user enters a page
|- Quiz results page & backend logic
|- fix links in navigation bar
|- Logic for quizes -> make the map section clickable
|- need a logic to find the what the next url is
    |- data.json has a timeline that specifies if the page is a lesson, question, or transition & corresponding id
    |- Idea: Have a function that parses the timeline and then renders appropriate url and content
|- transition template

## Second Priorities
|- update description on lessons
|- Make sure audio files are correct for each lesson
|- Enable/disable the next button until user answers question correctly
|- update alt for image descriptions


## Questions
- Update backend?
- What should the transition urls be?
